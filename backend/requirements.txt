Flask>=3.0
Flask-SQLAlchemy>=3.1
Flask-CORS>=4.0
Flask-JWT-Extended>=4.6
# Rate limiting για /api/auth/* (brute-force protection στο login)
Flask-Limiter>=3.5
python-dotenv>=1.0
# Production WSGI server (Render/Railway κ.λπ.) — το `python app.py` (Werkzeug
# dev server) ΔΕΝ πρέπει να τρέχει σε production.
gunicorn>=22.0
# Driver για Postgres — απαραίτητο όταν DATABASE_URL δείχνει σε postgres://
# (π.χ. managed Postgres του Render). Το SQLite default δεν το χρειάζεται.
psycopg2-binary>=2.9
# real_aade.py — πραγματική σύνδεση ΑΑΔΕ (XML build/validate + HTTPS)
lxml>=5.0
requests>=2.31
# Κρυπτογράφηση aade_subscription_key στη βάση (crypto.py)
cryptography>=42.0
# Error tracking (production) — δες config.SENTRY_DSN. Χωρίς αυτό, bugs σε
# production φαίνονται ΜΟΝΟ αν ψάξεις χειροκίνητα τα Render logs (δες πόσο
# αργά εντοπίστηκε το aade_state bug σήμερα, χωρίς αυτό).
sentry-sdk[flask]>=2.0
# Migrations βάσης (αντικαθιστά το χειροκίνητο _add_missing_columns) — δες
# migrations/README.md για το γιατί.
alembic>=1.13
